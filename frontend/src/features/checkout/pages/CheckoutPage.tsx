import { useState } from "react";
import type { FormEvent } from "react";

import { useNavigate } from "react-router-dom";
import { checkoutOrder } from "../api/onlineOrdersApi";
import "./CheckoutPage.css";
export function CheckoutPage() { const navigate = useNavigate(); const [error, setError] = useState(""); const [isSubmitting, setIsSubmitting] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); setIsSubmitting(true); setError(""); try { const order = await checkoutOrder({ recipientName: String(form.get("name")), recipientPhoneNumber: String(form.get("phone")), deliveryAddress: String(form.get("address")), paymentMethod: String(form.get("paymentMethod")) as "Cod" | "BankTransfer" }); navigate(`/orders/${order.onlineOrderId}`); } catch { setError("Không thể tạo đơn. Kiểm tra địa chỉ và tồn kho rồi thử lại."); } finally { setIsSubmitting(false); } } return <section className="checkout-page app-container"><h1>Thanh toán</h1><p>Phí giao hàng và tổng tiền được tính bởi hệ thống.</p><form onSubmit={(event) => void submit(event)}><label>Người nhận<input name="name" required /></label><label>Số điện thoại<input name="phone" inputMode="tel" required /></label><label>Địa chỉ giao hàng<textarea name="address" required rows={4} /></label><fieldset><legend>Thanh toán</legend><label><input defaultChecked name="paymentMethod" type="radio" value="Cod" /> Thanh toán khi nhận hàng</label><label><input name="paymentMethod" type="radio" value="BankTransfer" /> Chuyển khoản</label></fieldset>{error ? <p role="alert">{error}</p> : null}<button disabled={isSubmitting} type="submit">{isSubmitting ? "Đang tạo đơn…" : "Đặt hàng"}</button></form></section>; }
